```
xcode-select --install
sudo xcodebuild -license
cd ~
git clone https://github.com/yanamura/dotfiles.git ~/dotfiles
./dotfiles/scripts/install.sh
```

## emacs
M-x package-list-packages
で
helmとpopwinをいれる
